# BMC U-20A 格式与证据边界

参考资料：[BmcCpapData 原作者的格式研究](https://github.com/riaancillie/BmcCpapData)。本实现依据公开格式描述独立编写，没有复制第三方解析代码。

## 使用的字段

- USR `0x2296`：设备型号；当前只接受 U-20A。
- USR `0x2d`：空格填充的序列号；界面只在本地数据管理页显示，不写入公开导出说明。
- USR `0x102338`：历史记录数量；从 `0x102340` 起逐条验证 E1 标记和绝对 next 指针。
- 历史记录 `+7`：年月日位编码；`+15`：分钟数；`+69` 起为 5 字节索引，5 个 FF 后为带类型、计数和保留字段的消息。
- 83 / 84 / 87：OSA / HYP / CSA，每条 3 字节（中午起小时、分钟、记录秒数）。本软件保留分钟精度，并按设备摘要用机时长估算事件记录频率。
- 当前日 `0x431` 为日期，`0x441` 起解析消息，07 / 08 / 09 为 CSA / OSA / HYP。没有核实使用时长字段，因此不计算当前日事件指数。
- 编号文件：256 字节/秒，AAAA 头；248–254 保存完整年月日时分秒。逐包验证并按日期时间归档，从而绕开循环覆盖后失效的旧 idx 指针。
- 波形字节 `0x04` 与 `0x06`：参考 OSCAR legacy 解析分别解释为 EPAP 与 IPAP，原始值均按 `/2` 缩放。息览保留 `pressure` 兼容键（等同 `epap`），同时提供明确的 `epap`、`ipap` 通道。
- `+108 unsigned16[25] / 10`：原始流量 L/min，不擅自减基线或改作有符号。
- `+196 unsigned16 / 10`：设备漏气通道 L/min，是否扣除面罩排气未验证。
- `+198 unsigned16`：潮气量 mL；`+202 unsigned16 / 10`：分钟通气量 L/min；`+208 unsigned16`：呼吸频率 /min。

## 限制

公开研究基于多个 BMC 设备样本。本项目只把与当前 U-20A 文件结构相符的字段纳入解析；没有用 `.idx` 的设置区推断设备设置，也没有把未知压力通道当作医学处方、把未确认通道当作鼾声或血氧。

OSCAR legacy 的通道映射是当前双通道命名的参考依据，不是原厂认证。通道语义、单位、事件分类和统计口径仍需使用者与原厂软件逐项核对。波形包可能有重复时间戳或跳秒；解析保留有效包，重复时间戳作为诊断数量展示，不插值填补缺口。记录段数量不等同于开关机或醒来次数。

治疗日划分为 `[当日 12:00, 次日 12:00)`。`wave_seconds` 是有效波形包数的名义秒数，同一时间戳的有效包仍分别计入，不是按时间戳去重后的覆盖秒数。统计中位数和 P95 基于保留的有效包；原始摘要分钟数不会由波形包数替代。

## 验证边界

公开仓库只包含合成测试夹具和不含个人数据的示例截图。测试覆盖指针边界、日期、缩放、重复包、缺口、缺失摘要、导入回滚、报告转义与中英文导出；它不能证明任何具体患者数据的临床意义，也不能替代原厂软件或医生判断。

参考研究版本：[BmcCpapData](https://github.com/riaancillie/BmcCpapData)；OSCAR 相关公共实现见 [OSCAR-SQL](https://gitlab.com/CrimsonNape/OSCAR-SQL)。
